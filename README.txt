Put your files here:

- Isha-Nabeel-CV.pdf          -> goes directly in /assets, powers the "Download CV" button
- certificates/*.jpg          -> filenames already referenced in certificates.html / index.html
- achievements/*.jpg          -> filenames already referenced in index.html

Until you add an image, each flip card automatically shows a dashed
placeholder telling you the exact filename/path it's expecting.
